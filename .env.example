# WasteWise AI — environment template (NEVER commit real values)
# The prototype runs fully in DEMO MODE when these are absent.

# IBM Granite via watsonx.ai (production)
WATSONX_API_KEY=
WATSONX_PROJECT_ID=
GRANITE_MODEL=ibm/granite-3-8b-instruct

# Backend (production)
API_BASE_URL=http://localhost:3000/api
PORT=3000

# Database (production)
MONGODB_URI=

# Vision provider (production)
VISION_PROVIDER=mock
VISION_API_KEY=
